// Home page — the jungle layout handles the full-screen image & overlays.
// This page intentionally renders nothing so the background shows through.
export default function HomePage() {
  return null;
}
